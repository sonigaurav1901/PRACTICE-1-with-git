trigger contactTriggerMock on Contact (before insert,before update) {
	/*if(trigger.isbefore && trigger.isInsert) {
		helperClass.checkDuplicate(trigger.new);
	}
	else if(trigger.isbefore && trigger.isUpdate) {
		helperClass.checkDuplicate(trigger.new);
	}*/
}